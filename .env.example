# =====================================
#  local-mcp-starter — .env.example
# =====================================

# -------------------------------------
# Path Settings
# -------------------------------------

# Absolute path to the project on your host machine
# that you want the MCP server to access.
# Example: /home/<USERNAME>/my-project
HOST_PROJECT_PATH=/path/to/your/project

# Path inside the container where the host project
# will be mounted (read-only).
# This path is used by Docker only.
CONTAINER_PROJECT_PATH=/workspace/target-project

# The directory that the MCP server should treat as
# the project root when resolving paths.
# In most cases, keep this the same as CONTAINER_PROJECT_PATH.
PROJECT_ROOT=/workspace/target-project


# -------------------------------------
# MCP Server Settings
# -------------------------------------

# Name of the MCP server as recognized by tools like
# Cursor / ChatGPT / Claude Desktop.
MCP_SERVER_NAME=local-mcp-starter

# Docker image name for building and running the MCP server.
MCP_IMAGE_NAME=local-mcp-starter


# -------------------------------------
# Optional Settings
# (You may safely add more variables)
# -------------------------------------

# Any additional environment variables added here
# will be exported into the MCP container at runtime.
# For example:
# EXTRA_OPTION=true